# InCycling
Python tool that processes each provided file and produces a corresponding output file, with the same filename, but in a different directory.
My task was to write a Python tool that processes each file and produces a corresponding output file, with the same filename, but in a different directory (output directory). In these output files every chemical name should be accompanied by a matching CAS Registry Number, resolved via the PubChem public API.

